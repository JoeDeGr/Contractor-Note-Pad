<h1>Create a New Project!</h1>

<% if @project.errors.any? %>
    <div id="errors">
        <h3>Sorry, There were a few errors:</h2>
        <ul>
            <% @project.errors.full_messages.each do |m|%>
                <li><%= m %></li>
            <% end %>
        </ul>
    </div>
<% end %>

<%= render partial: "/projects/form" %>