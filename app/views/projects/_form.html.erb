   <div> Name: <%= text_field_tag "project[name]", (@project.name if @project) %></div><br>
   <div> Description: <%= text_area_tag "project[description]", (@project.description if @project) || "Enter your description here."%></div><br>
    <%= hidden_field_tag "project[user_id]", @user.id %>
    
